name: TheJERKTracker - GitHub Pages Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch: # Manual trigger available

# GitHub Pages deployment - independent of Vercel
# This workflow creates a static export for GitHub Pages hosting
# Repository: TeamPaintbrush/thejerktracker (MAIN)
# Last updated: September 22, 2025

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      CI: true
      GITHUB_ACTIONS: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install dependencies
        run: |
          npm ci --legacy-peer-deps
          
      - name: Pre-build validation
        run: |
          echo "Running pre-build validation..."
          node scripts/validate-build.js
        env:
          CI: true
          GITHUB_ACTIONS: true
          
      - name: Setup environment for static build
        run: |
          echo "Setting up environment for GitHub Pages static build..."
          
      - name: Setup minimal Prisma for static build
        run: |
          echo "Setting up minimal Prisma schema for static generation..."
          if [ -f "prisma/schema.github.prisma" ]; then
            cp prisma/schema.github.prisma prisma/schema.prisma
          fi
          npx prisma generate || echo "Prisma generate failed, continuing..."
          
      - name: Database check
        run: |
          echo "Running database connectivity check..."
          node scripts/db-check.js || echo "Database check completed with warnings"
        env:
          CI: true
          GITHUB_ACTIONS: true
        
      - name: Build Next.js app  
        run: npm run build:github
        env:
          GITHUB_ACTIONS: true
          CI: true
          NODE_ENV: production
          NEXT_PUBLIC_BASE_PATH: /thejerktracker
          DATABASE_URL: "file:./dummy.db"
          
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4